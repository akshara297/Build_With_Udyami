def countBits(n: int) -> list[int]:
    dp = [0] * (n + 1)
    
    for i in range(1, n + 1):
        # State transition: bits of shifted number + last bit status
        dp[i] = dp[i >> 1] + (i & 1)
        
    return dp
